global:
  scrape_interval: 15s
  evaluation_interval: 15s
  external_labels:
    monitor: "sla-guardian"

scrape_configs:
  - job_name: "sla-guardian-api"
    static_configs:
      - targets: ["api:3000"]
    metrics_path: "/metrics"
    scrape_interval: 15s
